# Goals
Build a freestyle rap trainer. Interprets user voice in real time and proposes rhymes for the following line.

## Do
- test all changes with `pytest -v`
- default to small files and diffs. avoid repo-wide rewrites unless asked

## Don't
- do not add new dependencies without approval
- do not worry about deprecation when refactoring
